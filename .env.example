# Base URL of the MIC Risk API.
# Leave empty in development: vite.config.ts proxies /api to http://localhost:5166, which keeps
# the browser on one origin so the SameSite=Strict refresh cookie behaves as it does in production.
VITE_API_BASE_URL=
